---
name: Menu Button
route: /menu-button
menu: Button
---

# Button

import { Playground, PropsTable } from 'docz'
import { MenuButtons } from 'synapsefi-dev-ui';
import FlexAlign from '../../common/FlexAlign';

## Basic

<FlexAlign style={{ justifyContent: 'flex-end' }}>
  <MenuButtons
    data={
      [
        { text: 'This will cause an Alert', onClick: () => alert('Test') },
        { text: 'This will cause a Console.log', onClick: () => console.log('Test') },
      ]
    }
  />
  
</FlexAlign>

```jsx
<MenuButtons
  data={
    [
      { text: 'This will cause an Alert', onClick: () => alert('Test') },
      { text: 'This will cause a Console.log', onClick: () => console.log('Test') },
    ]
  }
/>
```

## Props


| Property | Type | Required | Description |
| :------- | :--- | :------- | :---------- |
| data    | `array` | true   | List of menu items, needs "text" and "onClick" |
| className    | `string` | false   | Optional class name |

