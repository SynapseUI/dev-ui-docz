---
name: Button
route: /buttons
menu: Button
---

# Button

import { Button } from 'synapsefi-dev-ui';
import FlexAlign from '../../common/FlexAlign';

## Basic

<Button onClick={() => console.log('This is an alert')}>Click</Button>

```jsx
<Button onClick={() => console.log('This is an alert')}>Click</Button>
```

## Styles

<FlexAlign>
  <Button primary onClick={() => console.log('Primary')}>Primary</Button>
  <Button secondary onClick={() => console.log('Secondary')}>Secondary</Button>
  <Button tertiary onClick={() => console.log('Tertiary')}>Tertiary</Button>
</FlexAlign>

```jsx
<Button primary onClick={() => console.log('Primary')}>Primary</Button>
<Button secondary onClick={() => console.log('Secondary')}>Secondary</Button>
<Button tertiary onClick={() => console.log('Tertiary')}>Tertiary</Button>
```

## Props

| Property   | Type      | Required | Description                                        |
| :-------   | :---      | :------- | :----------                                        |
| className  | `string`  | false    | className of the Button                            |
| primary    | `boolean` | false    | Set Primary style                                  |
| secondary  | `boolean` | false    | Set Secondary style                                |
| tertiary   | `boolean` | false    | Set Tertiary style                                 |
| remove     | `boolean` | false    | Set Remove style                                   |
| fullWidth  | `boolean` | false    | Set Full Width style                               |
| isDisabled | `boolean` | false    | Set isDisabled style, will prevent on click action |
| isLoading  | `boolean` | false    | Set isLoading style, will prevent on click action  |
| onClick    | `function`| false    | The onclick function that gets trigged when clicked       |