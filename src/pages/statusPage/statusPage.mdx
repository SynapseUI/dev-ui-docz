---
name: StatusPage
route: /statuspage
---

# Status Page

import StatusPageContainer from './statusPage.state';

## Basic

<StatusPageContainer />

```jsx
hi
```

## Props

| Property  | Type       | Required | Description                   |
| :-------  | :---       | :------- | :----------                   |
| value     | `bool`     | true     | Value of StatusPage         |
| className | `string`   | false    | className of the StatusPage |
| onChange  | `function` | true     | Function triggered on toggle  |
| propName  | `string`   | false    | Name of the state key         |

 * @param {string} headerText - header text
 * @param {Object[]} iconData - main content item(s) between Header and Action Buttons (technically [optional])
 * @param {Component} iconData[].svg - icon for that item group
 * @param {string} iconData[].text - primary text in the group
 * @param {string} iconData[].subtext - secondary text in the group
 * @param {string} iconData[].itemStyle - string of css rendered on each item container
 * @param {Component} customContent - [optional] custom component that would display in the place of or below `<IconContent>`
 * @param {object} button - [optional] main button below the icon item
 * @param {string} button.text - button text
 * @param {function} button.onClick - function fired on button click
 * @param {object} additionalLink - [optional] link shown below the button
 * @param {string} additionalLink.text - link text
 * @param {function} additionalLink.onClick - function fired on link click
 * @param {boolean} hideNavbar - [optional] boolean that hides the NavBar, not needed if customNavbar included
 * @param {Component} customNavbar - [optional] custom component that replaces the white Onboarding Navbar
 * @param {boolean} coverFullScreen - [optional] makes the component cover the entire page instead of sitting in its parent

